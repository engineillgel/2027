# Job Management Demo (Spring Boot + Vue 3)

This repository contains a demo implementation of a campus job management system.

Structure:
- backend: Spring Boot application
- frontend: Vue 3 + Vite application

Run:
- Start MySQL and configure backend/src/main/resources/application.yml
- Start backend (mvn package -> java -jar)
- Start frontend (npm install -> npm run dev)
