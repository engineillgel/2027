server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # SPA 路由回退：直接访问 /login、/schedule 等路径时返回 index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
