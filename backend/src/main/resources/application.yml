spring:
  datasource:
    url: jdbc:mysql://localhost:3306/job_mgt?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: your_db_password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: ChangeThisSecretKeyForDemo_ChangeMe
  expiration-ms: 86400000
server:
  port: 8080
